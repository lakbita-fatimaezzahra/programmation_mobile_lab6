<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Pizza Recipes"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PizzaLak">

        <!-- Splash Screen -->
        <activity
            android:name="com.example.pizzalak.UI.PizzaSplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Liste des pizzas -->
        <activity
            android:name="com.example.pizzalak.UI.ListPizzaActivity"
            android:exported="false" />

        <!-- Détail pizza -->
        <activity
            android:name="com.example.pizzalak.UI.PizzaDetailActivity"
            android:exported="false" />

    </application>

</manifest>